{
  "name": "einstein-course-site",
  "private": true,
  "scripts": {
    "test": "python3 -m unittest discover -s tests -v && playwright test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  }
}
